let mover = {
    getDirection() {
        const availlableDirections = [2, 4, 6, 8];
        while (true) {
            let direction = parseInt(prompt('Введите число, куда вы хотите переместиться: 2: вниз, 4: влево, 6: вправо, 8: вверх, или "Отмена" для выхода'));
        }
    }
}